<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>
<%= image_tag @project.finished_image.url %>
<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>
<% if @project.user == current_user %>
<%= link_to 'Edit', edit_project_path(@project) %> |
<% end %>
<%= link_to 'Back', projects_path %>
